
# 🏢 HR Management System

Современная система управления персоналом с веб-интерфейсом, построенная на Node.js, Express и MySQL.

## ✨ Основные возможности

- 👥 **Управление сотрудниками** - полный CRUD для данных персонала
- 🔐 **Система ролей** - админ, редактор, пользователь
- 📊 **Справочники** - должности, профессии, образование
- 📜 **История действий** - учёт премий и штрафов
- 🛡️ **Безопасность** - JWT аутентификация, bcrypt шифрование
- 📱 **Современный UI** - адаптивный дизайн с Bootstrap
- 🐳 **Docker готов** - контейнеризация для лёгкого развертывания

## 🚀 Быстрый старт

### С помощью Docker (рекомендуется):
```bash
# Клонирование проекта
git clone <your-repo-url>
cd hr-management-system

# Запуск с Docker
docker-compose up -d

# Система доступна на http://localhost:3000
```

### Локальное развертывание:
```bash
# Установка зависимостей
npm install

# Настройка переменных окружения
cp .env.example .env
# Отредактируйте .env файл с вашими настройками

# Настройка базы данных MySQL
mysql -u root -p < ok.sql

# Запуск приложения
npm start
```

## 📁 Архитектура проекта

```
hr-management-system/
├── src/                    # Исходный код
│   ├── config/            # Конфигурация
│   │   └── database.js    # Настройки БД
│   ├── controllers/       # Контроллеры
│   │   ├── authController.js
│   │   ├── workerController.js
│   │   ├── referenceController.js
│   │   └── historyController.js
│   ├── middleware/        # Промежуточное ПО
│   │   ├── auth.js        # Аутентификация
│   │   └── validation.js  # Валидация
│   ├── routes/            # Маршруты API
│   │   ├── authRoutes.js
│   │   ├── workerRoutes.js
│   │   ├── referenceRoutes.js
│   │   └── historyRoutes.js
│   └── utils/             # Утилиты
│       └── logger.js      # Логирование
├── public/                # Статические файлы
│   ├── home.html         # Главная страница
│   ├── login.html        # Страница входа
│   ├── register.html     # Регистрация
│   ├── index.html        # Управление сотрудниками
│   ├── list.html         # История действий
│   └── tables.html       # Справочники
├── logs/                  # Файлы логов
├── server.js             # Точка входа
├── ok.sql               # Схема базы данных
├── docker-compose.yml   # Docker конфигурация
└── .env.example         # Пример переменных окружения
```

## 🔧 Технологический стек

**Backend:**
- Node.js + Express.js
- MySQL с пулом соединений
- JWT для аутентификации
- bcrypt для хэширования паролей
- Helmet.js для безопасности
- express-validator для валидации
- morgan для логирования

**Frontend:**
- Vanilla JavaScript
- Bootstrap 5
- Bootstrap Icons
- Адаптивный дизайн

**DevOps:**
- Docker & Docker Compose
- Структурированное логирование
- Rate limiting
- Health checks

## 🔐 Безопасность

- ✅ Хэширование паролей с bcrypt (соль 12 раундов)
- ✅ JWT токены с ограниченным временем жизни
- ✅ Защита от CSRF с Helmet.js
- ✅ Rate limiting для API endpoints
- ✅ Валидация всех входящих данных
- ✅ Управление доступом на основе ролей
- ✅ Безопасная конфигурация CORS
- ✅ Переменные окружения для секретов
- ✅ SQL injection защита с prepared statements

## 👤 Пользователи по умолчанию

| Логин | Пароль | Роль |
|-------|---------|------|
| admin | admin | admin |
| editor | editor | editor |
| user | user | user |

## 📊 API Endpoints

### Аутентификация
- `POST /login` - Вход в систему
- `POST /register` - Регистрация
- `GET /me` - Информация о пользователе
- `GET /verify` - Проверка токена

### Работники
- `GET /workers` - Список работников
- `POST /workers` - Создание работника
- `PUT /workers/:id` - Обновление работника
- `DELETE /workers/:id` - Удаление работника

### Справочники
- `GET /api/{positions|professions|education|genders|actions}` - Получение списка
- `POST /api/{reference}` - Создание записи
- `PUT /api/{reference}/:id` - Обновление записи
- `DELETE /api/{reference}/:id` - Удаление записи

### История действий
- `GET /list` - История действий
- `POST /list` - Добавление записи
- `PUT /list/:id` - Обновление записи
- `DELETE /list/:id` - Удаление записи

Полная документация API: [API_DOCUMENTATION.md](API_DOCUMENTATION.md)

## 📈 Мониторинг и логирование

Система ведёт подробные логи:
- HTTP запросы с временем ответа
- Операции аутентификации
- Ошибки базы данных
- Действия пользователей

Логи сохраняются в папке `/logs/` с ротацией по дням.

## 🔧 Переменные окружения

Создайте файл `.env` на основе `.env.example`:

```env
# Database
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=ok

# Security
JWT_SECRET=your-super-secret-key
JWT_EXPIRES_IN=24h

# Secrets for registration
ADMIN_SECRET=your-admin-secret
EDITOR_SECRET=your-editor-secret
```

## 📝 База данных

Схема включает таблицы:
- `users` - Пользователи системы
- `workers` - Данные сотрудников
- `position` - Должности
- `prof` - Профессии
- `education` - Образование
- `gender` - Пол
- `actions` - Типы действий (премии/штрафы)
- `list` - История действий

## 🚀 Производительность

- Connection pooling для базы данных
- Сжатие ответов (gzip)
- Оптимизированные SQL запросы
- Rate limiting защита от спама
- Кэширование статических файлов

## 🔄 Обновления и миграции

При обновлении с предыдущей версии:
1. Старые MD5 пароли автоматически обновляются на bcrypt при первом входе
2. Все новые маршруты обратно совместимы
3. Добавлены новые security заголовки

## 🤝 Участие в разработке

1. Fork проекта
2. Создайте feature branch (`git checkout -b feature/AmazingFeature`)
3. Commit изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в branch (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📜 Лицензия

Этот проект распространяется под лицензией MIT. См. `LICENSE` файл для деталей.

## 🆘 Поддержка

При возникновении проблем:
1. Проверьте логи в папке `/logs/`
2. Убедитесь в правильности переменных окружения
3. Проверьте подключение к базе данных
4. Создайте issue с описанием проблемы

---

**Разработано с ❤️ для эффективного управления персоналом**